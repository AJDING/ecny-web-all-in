default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: all_in_development

test:
  <<: *default
  database: all_in_test

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>
